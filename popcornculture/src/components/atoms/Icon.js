import React from "react";
import { FaUserAlt } from "react-icons/fa";

const Icon = ({ onIconClick, ...props }) => {
  return (
    <div>
      <FaUserAlt />
    </div>
  );
};

export default Icon;
